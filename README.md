# Swarm Factory

`swarm_factory` is a Rust crate responsible for launching agent factories. It relies on the `swarm` crate for core functionalities and agent definitions. This crate provides the necessary infrastructure to instantiate and manage a collection of intelligent agents.